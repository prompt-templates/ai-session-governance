# v2.8 Release Notes

Date: 2026-04-20 (UTC)

## Highlights

- Hardened the INIT-only installation boundary.
- Removed user-path references to non-installed internal tooling.
- Added regression guards to block future packaging drift.

## What changed

1. INIT packaging boundary
- Removed `FILE 7: docs/qa/session_log_maintenance.py` from `INIT.md` user install flow.
- Removed `INIT.md` references to non-installed tooling paths (for example `docs/qa/run_checks.sh`).

2. User-facing docs alignment
- Updated README (EN/zh-TW/zh-CN/ja) to keep session-log guidance user-facing only.
- Removed internal script/command mentions from README install narrative.
- Added `v2.8` release row in all 4 README files.

3. QA and verification updates
- Updated regression docs (`docs/qa/LATEST.md`, `docs/qa/QA_REGRESSION_REPORT.md`, `docs/VERIFICATION.md`) to current baseline.
- Added checks:
  - `R27-19`: INIT must not reference `docs/qa/run_checks.sh`
  - `R27-20`: INIT must not reference `docs/qa/session_log_maintenance.py`

## Validation

- Command: `bash docs/qa/run_checks.sh`
- Result: `232/232 PASS`

## Impact

- Prevents first-run errors for users who install using only `INIT.md`.
- Keeps README focused on user operations instead of internal maintainer tooling.
- Ensures future boundary regressions are auto-detected in regression QA.
